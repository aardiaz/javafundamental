package array;

public class ArrayDocs {
	
	/*
	 * ========================== Array =========================
	 * 
	 *   # use to store and processing homogeneous data.
	 *   # collection/group of multiple values of same data type 
	 *     is known as array.
	 *    
	 *   # array has fixed length/size.
	 *   # also contains duplicate data.
	 *   # data stored in the basis of indices(index starts from 0)
	 *   # contiguous memory allocation
	 *   # random access
	 *   # array implicitly treats as a object in java
	 *   
	 *    syntax :
	 *    
	 *    data_type  array_name[] = new data_type[size];
	 *    
	 *     e.g.
	 *     
	 *      String[]   cname = new String[50];
	 *      
	 *      char  []alphabets = new char[26];
	 *      
	 *      float  height[] = new float[5000];
	 *      
	 *      int age[] = new int[250];
	 *      
	 *      //Book  books[]  = new Book[900];
	 *      
	 */

}
